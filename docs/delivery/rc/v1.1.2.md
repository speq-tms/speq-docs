# Release candidate v1.1.2

Generated by `scripts/rc-report.sh` from GitHub state on 2026-08-21 16:13 UTC.
Do not edit by hand — regenerate instead.

Progress: **1 of 1** issues closed.

## Scope

Issues on milestone `v1.1.2` in [`speq-tms/speq-docs`](https://github.com/speq-tms/speq-docs/issues).

| Issue | Area | State | Title |
| --- | --- | --- | --- |
| #54 | area/release | closed | [bug] выпущенный бинарь представляется версией из Cargo.toml, а её не поднимают при релизе |

## Changes per repository

### speq-cli

- [#30](https://github.com/speq-tms/speq-cli/pull/30) fix(release): bump the package version and refuse a mismatched tag

### speq-docs

- [#55](https://github.com/speq-tms/speq-docs/pull/55) docs(release): make the package version a release gate

## Rollout order

Merge each RC into `main` in this order, and only after every issue on the milestone is closed.
The order is a dependency chain, not a preference.

1. **speq-cli** — runtime; must match the contracts above
2. **speq-docs** — documents what actually shipped

> `homebrew-tap` must not be merged before the `speq-cli` release artifact exists.
> In `speq-cli`, merging a branch whose name starts with `v` into `main` triggers the release build.

## Release gates

- [ ] Every issue on milestone `v1.1.2` is closed
- [ ] `cargo build && cargo test` green in `speq-cli`
- [ ] The version in `speq-cli/Cargo.toml` equals the tag being published
- [ ] `speq-examples/test-repo-mode-jsonplaceholder` reports `"failed": 0`
- [ ] Every new feature has an acceptance example
- [ ] Documentation matches implemented behavior
- [ ] Contracts aligned across `speq-cli`, `speq-contracts`, `speq-vscode-extension`
